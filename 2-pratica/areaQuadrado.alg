algoritmo "areaQuadrado"
var lado, a: inteiro

inicio
    escreva("Qual o tamanho do lado do quadrado? ")
    leia(lado)
    escreval(lado)
    a <- lado^2
    escreva("A área do quadrado é de ", a)
fimalgoritmo