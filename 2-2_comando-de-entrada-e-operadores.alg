algoritmo "operadores"
var a: inteiro
    b, c, d: real
inicio
    a <- Abs(-50)
    b <- Exp(9, 5)
    c <- int(RaizQ(90))
    d <- Sen(GraupRad(90))
    escreval(a)
    escreval(b)
    escreval(c)
    escreval(d)
fimalgoritmo